# Docker Environment Configuration for PostgreSQL Testing
# This file contains environment variables for running the application with PostgreSQL in Docker

# Database Configuration - PostgreSQL
DATABASE_TYPE=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=dbuser
DATABASE_PASSWORD=dbpassword
DATABASE_NAME=mydb
DATABASE_SCHEMA=public

# Application Configuration
NODE_ENV=development
PORT=3300

# Logging Configuration
LOG_LEVEL=debug

# Docker-specific PostgreSQL Configuration
# These variables are used by the PostgreSQL Docker container
POSTGRES_DB=mydb
POSTGRES_USER=dbuser
POSTGRES_PASSWORD=dbpassword
POSTGRES_INITDB_ARGS=--auth-host=scram-sha-256 --auth-local=scram-sha-256

# Alternative MySQL Configuration (for comparison/switching)
# Uncomment these and comment PostgreSQL variables above to use MySQL
# DATABASE_TYPE=mysql
# DATABASE_HOST=localhost
# DATABASE_PORT=3306
# DATABASE_USER=dbuser
# DATABASE_PASSWORD=dbpassword
# DATABASE_NAME=mydb